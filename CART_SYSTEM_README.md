# 🛒 Система управления корзиной товаров

> **Полное решение** для добавления товаров в корзину с авторизацией, синхронизацией с сервером и управлением количеством товаров.

## 📋 Краткое резюме

Я создал полную систему управления корзиной для вашего приложения "Simple Shopper". Вот что было сделано:

### ✨ Frontend компоненты

| Файл | Описание |
|------|---------|
| **AddToCart.vue** | Основной компонент с двумя состояниями: кнопка → счётчик |
| **cart.interface.ts** | TypeScript интерфейсы для корзины |
| **cart.state.ts** | Pinia store для управления состоянием корзины |

### 🌐 Backend API

| Маршрут | Метод | Описание |
|---------|-------|---------|
| `/api/cart` | `POST` | Добавить товар в корзину |
| `/api/cart` | `GET` | Получить корзину пользователя |
| `/api/cart` | `DELETE` | Удалить товар из корзины |

### 🗄️ База данных

Новая таблица `cart_items` с полями:
- `id` - первичный ключ
- `user_id` - ID пользователя
- `product_id` - ID товара
- `quantity` - количество товаров

---

## 🚀 Быстрый старт

### 1️⃣ Используйте компонент на странице товара

```vue
<template>
  <div class="product-page">
    <h1>{{ product.name }}</h1>
    <p>{{ product.price }} ₽</p>
    
    <!-- Вставляем компонент -->
    <AddToCart :product="product" />
  </div>
</template>

<script setup lang="ts">
import type { Product } from '~/interfaces/product.interface'
const { data: product } = await useFetch<Product>(`/api/products/${id}`)
</script>
```

### 2️⃣ Используйте store в других компонентах

```typescript
const cartStore = useCartStore()

// Информация о корзине
const itemsCount = cartStore.getItemsCount()      // Количество товаров
const totalPrice = cartStore.getTotalPrice()      // Сумма корзины
const items = cartStore.cartItems                 // Все товары

// Управление корзиной
cartStore.removeFromCart(productId)               // Удалить товар
cartStore.updateQuantity(productId, 5)            // Изменить количество
cartStore.clearCart()                             // Очистить корзину
```

### 3️⃣ Backend готов!

- Таблица `cart_items` создастся автоматически
- Маршруты уже добавлены в `main.go`
- Обработчики API в `handlers/cart.go`

---

## 📚 Документация

| Документ | Содержание |
|----------|-----------|
| **[CART_SYSTEM_DOCUMENTATION.md](./CART_SYSTEM_DOCUMENTATION.md)** | Полная документация системы |
| **[IMPLEMENTATION_CHECKLIST.md](./IMPLEMENTATION_CHECKLIST.md)** | Пошаговый чеклист внедрения |
| **[VISUAL_GUIDE.md](./VISUAL_GUIDE.md)** | Визуальный гайд компонента |
| **[API_REQUESTS_EXAMPLES.js](./API_REQUESTS_EXAMPLES.js)** | Примеры HTTP запросов |
| **[USAGE_EXAMPLE.vue](./frontend/USAGE_EXAMPLE.vue)** | Пример использования |
| **[PRODUCT_PAGE_EXAMPLE.vue](./frontend/PRODUCT_PAGE_EXAMPLE.vue)** | Пример страницы товара |
| **[CART_PAGE_EXAMPLE.vue](./frontend/CART_PAGE_EXAMPLE.vue)** | Пример страницы корзины |

---

## 🎨 Компонент AddToCart работает в двух режимах

### 🔴 Режим 1: Неавторизованный / Начальное состояние

```
┌──────────────────────────┐
│ Добавить в корзину       │
│ (черная кнопка)          │
└──────────────────────────┘
  ↓ При клике (не авторизован)
Редирект на /auth/login 🔐
  ↓ При клике (авторизован)
Переход в режим 2
```

### 🟢 Режим 2: Авторизованный / Выбор количества

```
┌─────────┬──────────┬─────────┐
│ Удалить │ Счётчик  │ Добавить│
└─────────┴──────────┴─────────┘
  ↓ При клике "Удалить"
Возврат в режим 1
  ↓ При клике "Добавить"
Сохранение товара → Возврат в режим 1
```

---

## 💾 Где сохраняется информация о товаре?

### 🖥️ Локально (браузер)
- **Хранилище:** `localStorage`
- **Синхронизация:** автоматическая (через `persist: true`)
- **Время жизни:** пока не очистить кеш браузера

### ☁️ На сервере (авторизованные пользователи)
- **Хранилище:** таблица `cart_items` в БД
- **Синхронизация:** двусторонняя (браузер ↔ сервер)
- **Восстановление:** при загрузке страницы (функция `restore()`)

### 🔄 Поток синхронизации

```
Добавить товар в браузере
  ↓
cartStore.addToCart(product, quantity)
  ↓
├─ Обновить localStorage
└─ POST /api/cart (если авторизован)
  ↓
Сохранено ✅
```

---

## 🔍 Как проверить в браузере (DevTools)

### Console (F12)
```javascript
// Получить store
const cartStore = useCartStore()
const authStore = useAuthStore()

// Проверить токен
console.log(authStore.token)      // Должен быть токен

// Проверить корзину
console.log(cartStore.cartItems)  // Массив товаров
console.log(cartStore.getItemsCount())    // Количество
console.log(cartStore.getTotalPrice())    // Сумма
```

### Application → LocalStorage
```
Ключ: "cart"
Значение: {"cartItems": [...]}
```

### Network (F12)
```
Должны быть запросы:
✅ POST /api/cart (при добавлении)
✅ GET /api/cart (при загрузке)
✅ DELETE /api/cart (при удалении)
```

---

## 🧪 Примеры использования

### Добавление товара в корзину

```vue
<script setup lang="ts">
const cartStore = useCartStore()
const product = ref({ id: 1, name: 'Кольцо', price: 1999, ... })

function addItem() {
  cartStore.addToCart(product.value, 2)  // Добавляем 2 штуки
}
</script>

<template>
  <button @click="addItem">Добавить в корзину</button>
</template>
```

### Отображение корзины

```vue
<script setup lang="ts">
const cartStore = useCartStore()

onMounted(async () => {
  // Загрузить корзину с сервера
  if (authStore.email) {
    await cartStore.restore(authStore.email)
  }
})
</script>

<template>
  <div class="cart">
    <div v-for="item in cartStore.cartItems" :key="item.product.id">
      <h3>{{ item.product.name }}</h3>
      <p>{{ item.quantity }} × {{ item.product.price }} ₽</p>
      <button @click="cartStore.removeFromCart(item.product.id)">Удалить</button>
    </div>
    <p class="total">Итого: {{ cartStore.getTotalPrice() }} ₽</p>
  </div>
</template>
```

### Показ количества товаров в header

```vue
<template>
  <header>
    <div class="logo">Simple Shopper</div>
    <nav>...</nav>
    <div class="cart-icon">
      🛒
      <span v-if="cartStore.getItemsCount() > 0" class="badge">
        {{ cartStore.getItemsCount() }}
      </span>
    </div>
  </header>
</template>

<script setup lang="ts">
const cartStore = useCartStore()
</script>
```

---

## ⚙️ API Endpoints

### POST /api/cart - Добавить товар

**Request:**
```json
{
  "email": "user@example.com",
  "productID": 1,
  "quantity": 2
}
```

**Response:**
```json
{
  "success": true,
  "message": "Item added to cart"
}
```

### GET /api/cart - Получить корзину

**Query:** `?email=user@example.com`

**Response:**
```json
[
  {
    "product": { /* полные данные товара */ },
    "quantity": 2
  }
]
```

### DELETE /api/cart - Удалить товар

**Request:**
```json
{
  "email": "user@example.com",
  "productID": 1
}
```

**Response:**
```json
{
  "success": true,
  "message": "Item removed from cart"
}
```

---

## 📂 Структура файлов

```
frontend/
├── components/
│   ├── AddToCart.vue          ← Новый компонент ✨
│   ├── ActionButton.vue       (уже существует)
│   └── CounterFiled.vue       (уже существует)
├── interfaces/
│   └── cart.interface.ts      ← Новый интерфейс ✨
├── state/
│   ├── cart.state.ts          ← Новый store ✨
│   ├── auth.state.ts          (существует)
│   └── favorite.state.ts      (существует)
├── pages/
│   ├── cart.vue               (обновить примером)
│   └── catalog/
│       └── sup-[id].vue       (добавить компонент)

backend/
├── handlers/
│   └── cart.go                ← Новые обработчики ✨
├── main.go                    ← Обновлены маршруты ✨
└── database/
    ├── database.go            ← Обновлена таблица ✨
    └── migration_cart.sql     ← SQL миграция (опционально)

Документация:
├── CART_SYSTEM_DOCUMENTATION.md       ← Полная документация ✨
├── IMPLEMENTATION_CHECKLIST.md        ← Чеклист внедрения ✨
├── VISUAL_GUIDE.md                    ← Визуальный гайд ✨
├── API_REQUESTS_EXAMPLES.js           ← Примеры запросов ✨
├── PRODUCT_PAGE_EXAMPLE.vue           ← Пример страницы товара ✨
├── CART_PAGE_EXAMPLE.vue              ← Пример страницы корзины ✨
└── USAGE_EXAMPLE.vue                  ← Пример использования ✨
```

---

## 🎯 Следующие шаги

1. **Просмотрите** [IMPLEMENTATION_CHECKLIST.md](./IMPLEMENTATION_CHECKLIST.md)
2. **Интегрируйте** AddToCart компонент на страницу товара
3. **Обновите** страницу корзины (используйте пример)
4. **Протестируйте** добавление/удаление товара
5. **Убедитесь** в синхронизации с сервером (вкладка Network в DevTools)

---

## ❓ Часто задаваемые вопросы

**Q: Что произойдёт, если я не авторизован?**
A: Товар добавится в локальную корзину (localStorage), но не будет синхронизирован с сервером. При авторизации корзина загрузится с сервера.

**Q: Как скрыть компонент, если товар уже в корзине?**
A: Используйте `cartStore.getQuantity(productId)`:
```typescript
<AddToCart v-if="!cartStore.getQuantity(product.id)" :product="product" />
```

**Q: Как очистить корзину?**
A: `cartStore.clearCart()`

**Q: Таблица cart_items не создана?**
A: Удалите `app.db` и перезагрузите сервер.

---

## 📞 Поддержка

Если возникнут вопросы, обратитесь к:
- 📚 [CART_SYSTEM_DOCUMENTATION.md](./CART_SYSTEM_DOCUMENTATION.md) - полная документация
- 🖼️ [VISUAL_GUIDE.md](./VISUAL_GUIDE.md) - визуальные примеры
- 💻 [API_REQUESTS_EXAMPLES.js](./API_REQUESTS_EXAMPLES.js) - примеры запросов

---

## ✅ Что готово

- ✅ Frontend компоненты и store
- ✅ Backend API обработчики
- ✅ База данных (таблица и синхронизация)
- ✅ Полная документация
- ✅ Примеры использования
- ✅ Примеры HTTP запросов

**Система полностью готова к использованию!** 🎉

