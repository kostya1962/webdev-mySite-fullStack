# Чеклист внедрения системы корзины 🛒

## ✅ Что уже готово

### Frontend
- [x] `interfaces/cart.interface.ts` - интерфейсы для работы с корзиной
- [x] `state/cart.state.ts` - Pinia store для управления состоянием корзины
- [x] `components/AddToCart.vue` - основной компонент для добавления товара
- [x] `USAGE_EXAMPLE.vue` - пример использования компонента
- [x] `PRODUCT_PAGE_EXAMPLE.vue` - пример страницы товара
- [x] `CART_PAGE_EXAMPLE.vue` - пример страницы корзины

### Backend
- [x] `handlers/cart.go` - обработчики API для корзины
- [x] `main.go` - маршруты для API корзины
- [x] `database/database.go` - таблица cart_items в БД
- [x] `database/migration_cart.sql` - SQL миграция (если нужна)

### Документация
- [x] `CART_SYSTEM_DOCUMENTATION.md` - полная документация системы

---

## 🔧 Пошаговое внедрение

### Шаг 1: Frontend подготовка ✨

Все компоненты уже созданы! Они должны быть автоматически доступны благодаря Nuxt auto-import.

```bash
# Проверьте, что компоненты загружаются (в консоли браузера не должно быть ошибок)
# 1. AddToCart.vue
# 2. CounterFiled.vue (должен быть у вас)
# 3. ActionButton.vue (должен быть у вас)
```

### Шаг 2: Backend подготовка 🚀

#### 2.1. Убедитесь, что файл `handlers/cart.go` добавлен
```bash
ls -la backend/handlers/cart.go
# Должен существовать и содержать функции: AddToCart, GetCart, RemoveFromCart
```

#### 2.2. Проверьте маршруты в `main.go`
```bash
# Ищите эти строки:
# cart := api.Group("/cart")
# cart.Post("/", handlers.AddToCart)
# cart.Get("/", handlers.GetCart)
# cart.Delete("/", handlers.RemoveFromCart)
```

#### 2.3. База данных будет создана автоматически
При запуске backend'а функция `createTables()` создаст таблицу `cart_items` автоматически.

```bash
# Проверить:
cd backend
go run main.go

# Должно вывести:
# Database connected successfully
# Server starting on :3000
```

### Шаг 3: Интеграция на странице товара 🎯

Откройте вашу страницу товара (например, `frontend/pages/catalog/sup-[id].vue`):

```vue
<script setup lang="ts">
import type { Product } from '~/interfaces/product.interface'

const route = useRoute()
const productId = Array.isArray(route.params.id) 
  ? route.params.id[0].replace('sup-', '') 
  : route.params.id.replace('sup-', '')

const { data: product } = await useFetch<Product>(
  `/api/products/${productId}`
)
</script>

<template>
  <div class="product-page">
    <!-- Ваш контент товара -->
    <h1>{{ product.name }}</h1>
    <p>{{ product.price }} ₽</p>
    
    <!-- ДОБАВЬТЕ ЭТУ СТРОКУ: -->
    <AddToCart :product="product" />
  </div>
</template>
```

### Шаг 4: Обновление страницы корзины 🛍️

Откройте `frontend/pages/cart.vue` и замените содержимое на пример из `CART_PAGE_EXAMPLE.vue`.

Ключевые части:
```typescript
const cartStore = useCartStore()
const authStore = useAuthStore()

onMounted(async () => {
  if (authStore.email) {
    await cartStore.restore(authStore.email)
  }
})
```

### Шаг 5: Тестирование 🧪

#### Frontend тестирование:
```bash
cd frontend
npm run dev

# 1. Откройте страницу товара
# 2. Нажмите "Добавить в корзину"
# 3. Если не авторизованы → редирект на логин
# 4. Если авторизованы → появится счётчик
# 5. Выберите количество и нажмите "Добавить"
# 6. Компонент вернётся в исходное состояние
```

#### Backend тестирование:
```bash
# Используйте Postman или curl:

# 1. Добавить в корзину:
curl -X POST http://localhost:3000/api/cart \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "productID": 1,
    "quantity": 2
  }'

# 2. Получить корзину:
curl http://localhost:3000/api/cart?email=user@example.com

# 3. Удалить из корзины:
curl -X DELETE http://localhost:3000/api/cart \
  -H "Content-Type: application/json" \
  -d '{
    "email": "user@example.com",
    "productID": 1
  }'
```

---

## ❓ Часто задаваемые вопросы

### Q: Где сохраняется информация о товаре?
**A:** 
- Локально: в `localStorage` браузера (через `persist: true` в store)
- На сервере: в таблице `cart_items` БД (только для авторизованных)

### Q: Что произойдёт, если я не авторизован?
**A:** Товар всё ещё добавится в корзину браузера (localStorage), но не будет синхронизирован с сервером. При авторизации данные загрузятся с сервера.

### Q: Как удалить всю корзину?
**A:** 
```typescript
const cartStore = useCartStore()
cartStore.clearCart()
```

### Q: Как проверить количество товара в корзине на другой странице?
**A:**
```typescript
const cartStore = useCartStore()
const count = cartStore.getItemsCount()  // количество товаров
const total = cartStore.getTotalPrice()  // сумма
```

### Q: Что делать, если таблица `cart_items` не создалась?
**A:** 
```bash
# 1. Удалите файл app.db
rm backend/app.db

# 2. Перезагрузите сервер
cd backend
go run main.go

# Таблица создастся автоматически
```

---

## 📞 Проблемы и решения

| Проблема | Решение |
|----------|---------|
| AddToCart компонент не видится | Убедитесь, что `components/AddToCart.vue` в правильной папке |
| Ошибка "Product not found" в API | Проверьте, что product.id корректный |
| Товар не добавляется | Проверьте console.log в браузере на ошибки |
| Таблица cart_items не создана | Удалите app.db и перезагрузите сервер |
| Корзина не восстанавливается после авторизации | Вызовите `cartStore.restore(email)` после входа |

---

## 📚 Дополнительная информация

- Полная документация: `CART_SYSTEM_DOCUMENTATION.md`
- Пример использования компонента: `USAGE_EXAMPLE.vue`
- Пример страницы товара: `PRODUCT_PAGE_EXAMPLE.vue`
- Пример страницы корзины: `CART_PAGE_EXAMPLE.vue`

---

## 🎉 Готово!

Когда вы выполните все шаги, система управления корзиной будет полностью работоспособна!

**Контрольная точка:** товар добавляется в корзину, информация сохраняется, количество правильно отображается.

Если возникнут вопросы, обратитесь к документации или используйте инструменты разработки браузера (F12 → Console, Network).
